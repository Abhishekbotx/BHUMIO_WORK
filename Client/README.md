# Client (Vite + React + Tailwind)

Single-page UI for the transactions assignment.

## Run

```bash
npm install
npm run dev
```

## Backend API used

- `POST /api/v1/transactions`
- `GET /api/v1/transactions`

The base URL is defined in `src/services/transactions.ts`.
